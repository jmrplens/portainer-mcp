version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - copyloopvar
    - errorlint
    - gosec
    - misspell
    - nilerr
    - noctx
    - revive
    - unconvert
    - unparam
    - usestdlibvars
    - wrapcheck
  settings:
    wrapcheck:
      ignore-sigs:
        - .Errorf(
        - errors.New(
        - errors.Join(
        - .Wrap(
  exclusions:
    rules:
      # Table-driven tests routinely ignore returned errors on setup helpers,
      # and duplicated literals in fixtures are intentional.
      - path: _test\.go$
        linters:
          - gosec
          - wrapcheck
      # Generated from Portainer's OpenAPI spec; not hand-written, not ours to style.
      - path: internal/portainer/gen/
        linters:
          - revive
          - gosec
          - wrapcheck
          - unparam
          - errorlint
          - misspell
          - unconvert
          - usestdlibvars

formatters:
  enable:
    - gofumpt
    - goimports
    - gci
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/jmrplens/portainer-mcp)
